(test
  (name test_scanning)
  (modules test_scanning)
  (libraries alcotest scanning))

(test
  (name test_parsing)
  (modules test_parsing)
  (libraries alcotest parsing util))

(test
  (name test_evaluation)
  (modules test_evaluation)
  (libraries alcotest evaluation util))
